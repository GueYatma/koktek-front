name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Récupération du code
        uses: actions/checkout@v2

      - name: 2. Préparer les infos de build
        run: |
          echo "VITE_BUILD_ID=${GITHUB_SHA}" >> $GITHUB_ENV
          echo "VITE_BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV
          printf "VITE_BUILD_MESSAGE<<EOF\n%s\nEOF\n" "$(git log -1 --pretty=%s)" >> $GITHUB_ENV

      - name: 3. Construction sur GitHub (Build)
        # On prépare le site ici, sur les serveurs de GitHub
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_API_TOKEN: ${{ secrets.VITE_API_TOKEN }}
          VITE_PROJECT_NAME: ${{ secrets.VITE_PROJECT_NAME }}
          VITE_DIRECTUS_URL: ${{ secrets.VITE_DIRECTUS_URL }}
          VITE_DIRECTUS_TOKEN: ${{ secrets.VITE_DIRECTUS_TOKEN }}
          VITE_N8N_WEBHOOK_URL: ${{ secrets.VITE_N8N_WEBHOOK_URL }}
          VITE_N8N_STRIPE_INTENT_URL: ${{ secrets.VITE_N8N_STRIPE_INTENT_URL }}
          VITE_STRIPE_PUBLIC_KEY: ${{ secrets.VITE_STRIPE_PUBLIC_KEY }}
        run: |
          cat <<EOF > .env.production
          VITE_API_URL=$VITE_API_URL
          VITE_API_TOKEN=$VITE_API_TOKEN
          VITE_PROJECT_NAME=$VITE_PROJECT_NAME
          VITE_DIRECTUS_URL=$VITE_DIRECTUS_URL
          VITE_DIRECTUS_TOKEN=$VITE_DIRECTUS_TOKEN
          VITE_N8N_WEBHOOK_URL=$VITE_N8N_WEBHOOK_URL
          VITE_N8N_STRIPE_INTENT_URL=$VITE_N8N_STRIPE_INTENT_URL
          VITE_STRIPE_PUBLIC_KEY=$VITE_STRIPE_PUBLIC_KEY
          EOF
          npm install
          npm run build

      - name: 4. Préparer un dossier de déploiement temporaire
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 90s
          proxy_timeout: 90s
          command_timeout: 15m
          script: |
            set -e
            NEW_DIR="/root/koktek_front/dist_tmp_${{ github.sha }}"
            rm -rf "${NEW_DIR}"
            mkdir -p "${NEW_DIR}"
            echo "Dossier temporaire prêt: ${NEW_DIR}"

      - name: 5. Envoi du nouveau site (SCP -> dossier temporaire)
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 90s
          command_timeout: 15m
          source: "dist/*"
          target: "/root/koktek_front/dist_tmp_${{ github.sha }}/"
          strip_components: 1
          # strip_components: 1 permet de copier le CONTENU du dossier dist directement à la racine de la cible

      - name: 6. Synchronisation vers le dossier live (rsync)
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          timeout: 90s
          proxy_timeout: 90s
          command_timeout: 15m
          script: |
            set -e
            NEW_DIR="/root/koktek_front/dist_tmp_${{ github.sha }}"
            LIVE_DIR="/root/koktek_front/dist"

            if [ ! -d "${NEW_DIR}" ]; then
              echo "Dossier temporaire introuvable: ${NEW_DIR}"
              exit 1
            fi

            # Installer rsync si absent
            which rsync > /dev/null 2>&1 || apt-get update -qq && apt-get install -y -qq rsync

            # rsync remplace le CONTENU du dossier sans changer l'inode du dossier parent.
            # C'est essentiel pour les bind mounts Docker : le container Nginx continue
            # de pointer vers le même dossier et voit immédiatement les nouveaux fichiers.
            mkdir -p "${LIVE_DIR}"
            rsync -a --delete "${NEW_DIR}/" "${LIVE_DIR}/"
            rm -rf "${NEW_DIR}"
            echo "Synchronisation terminée."
